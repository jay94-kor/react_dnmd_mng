{
    "name": "project-management",
    "private": true,
    "version": "1.0.0",
    "scripts": {
      "dev": "pnpm -r --parallel dev",
      "build": "pnpm -r build",
      "lint": "pnpm -r lint"
    },
    "dependencies": {
      "eslint-plugin-react-refresh": "^0.4.14"
    }
  }
